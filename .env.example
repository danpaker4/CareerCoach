# Docker Compose ports are bound to localhost only.
MONGODB_HOST_PORT=27018
MONGOT_HEALTH_PORT=8080
MONGOT_METRICS_PORT=9946

# Change this outside local development. It is used only for mongot-to-mongod synchronization.
MONGOT_PASSWORD=careercoach-local-mongot-password

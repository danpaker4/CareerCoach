# Copy to the repo root as `.env` on the VM (next to docker-compose.prod.yml). Never commit the real file.
# MinIO credentials — generate strong values; must match S3_ACCESS_KEY_ID/S3_SECRET_ACCESS_KEY in users-service.env.
MINIO_ROOT_USER=
MINIO_ROOT_PASSWORD=
MINIO_BUCKET=careercoach-uploads

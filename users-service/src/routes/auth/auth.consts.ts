export const AUTH_ROUTE_PREFIX = "/api/auth";

export const AUTH_ROUTE_PATHS = {
  register: "/register",
  login: "/login",
  refresh: "/refresh",
  session: "/session",
  logout: "/logout",
} as const;
